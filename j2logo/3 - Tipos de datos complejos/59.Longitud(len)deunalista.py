# Como cualquier tipo secuencia, para conocer la longitud de una lista en Python se hace uso de la función len(). 
# Esta función devuelve el número de elementos de una lista:

>>> vocales = ['a', 'e', 'i', 'o', 'u']
>>> len(vocales)
5
